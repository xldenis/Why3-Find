(executable
  (name main)
  (public_name why3find)
  (libraries unix dune-site yojson))

(rule
  (target config.ml)
  (action
    (write-file %{target}
      "let version = \"%{version:why3find}\"")))

(generate_sites_module
  (module global)
  (sites why3find))
