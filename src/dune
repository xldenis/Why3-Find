(executable
  (name main)
  (package why3find)
  (public_name why3find)
  (flags (:standard "-w" "-9" "-open" "Why3utils"))
  (libraries why3find.lib str unix why3))

(rule
  (target config.ml)
  (action
    (write-file %{target}
      "let version = \"%{version:why3find}\"")))

(ocamllex lexer)
