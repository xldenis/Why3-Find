(executable
  (public_name why3find)
  (name main))

(rule
  (target version.ml)
  (action
    (write-file %{target}
      "let version = \"%{version:why3find}\"")))

(generate_sites_module
  (module global)
  (sites why3find))
