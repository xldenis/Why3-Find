module Add
  use int.Int
  constant zero : int = 0
  clone a.foo.Neutral with type t = int, constant e = zero, function op = (+)
end

module Mul
  use int.Int
  constant one : int = 1
  clone a.foo.Neutral with type t = int, constant e = one, function op = (*)
end
